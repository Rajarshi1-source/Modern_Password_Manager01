# ============================================================================
# Password Manager - Backend Dependencies
# ============================================================================
# Django 5.1.x (Latest Stable)
# ============================================================================

# Core Django Framework
Django==5.2.17
djangorestframework==3.17.2
djangorestframework-simplejwt==5.5.1
fido2>=1.1.3
# asgiref>=3.9.1 required by channels-redis 4.3.0 (Django 5.1 allows <4,>=3.8.1)
asgiref==3.9.1

# Django Extensions & Tools
django-extensions==4.1
django-cors-headers==4.9.0
django-storages==1.14.6
django-timezone-field==7.2.2
drf-yasg==1.21.15

# Authentication & OAuth 2.0 / OpenID Connect
django-allauth==65.14.1
dj-rest-auth==7.0.1
python3-openid==3.2.0
oauthlib==3.2.2
requests-oauthlib==2.0.0

# OpenID Connect (OIDC) Support
mozilla-django-oidc>=4.0.0,<5

# Cryptography & Security
# Pin 48.0.1 to fix GHSA-537c-gmf6-5ccf (the PyPI wheels bundle OpenSSL; <48.0.1
# ships the build hit by the 2026-06-09 advisory). Stay on 48.x, not 49.x:
# pyOpenSSL 26.x requires cryptography<49. The exact pin below is what enforces
# this — the pyOpenSSL>=26.0.0 floor does not cap cryptography on its own.
# Declined: GHSA-g6cj-pr64-35w5 / CVE-2026-69247 (Bleichenbacher oracle in
# PKCS#7 EnvelopedData decrypt, fixed 50.0.0) is not reachable — this codebase
# never calls pkcs7_decrypt_der/_pem/_smime — and taking it would break the
# pyOpenSSL<49 constraint above. Revisit alongside a pyOpenSSL major bump.
cryptography==48.0.1
argon2-cffi==21.3.0
argon2-cffi-bindings==25.1.0
pyOpenSSL>=26.0.0
pycryptodome>=3.20.0
pyotp==2.10.0

# JWT & Tokens
PyJWT==2.13.0
#python-jose-cryptodome>=1.3.2     # Not compatible with pycryptodome 3.20.0

# Celery & Task Queue
celery==5.6.3
django-celery-beat==2.9.0
django_celery_results==2.6.0
amqp==5.3.1
billiard==4.2.1
kombu==5.6.2
vine==5.1.0
redis==7.0.1

# Database — psycopg3 (async-capable successor to psycopg2)
psycopg[binary]==3.2.4
sqlparse==0.6.0

# HTTP & Networking
requests==2.33.0
certifi>=2026.4.22
charset-normalizer==3.4.1
idna==3.18
urllib3==2.7.0
dnspython==2.7.0

# Tor transport for Dark Protocol anonymous vault access.
#   stem     — reads live Tor state over the control port (bootstrap, circuits,
#              onion descriptor). Without it the capability reports Unavailable.
#   PySocks  — SOCKS5 support for requests, used by the onion loopback
#              self-check (socks5h, so the .onion resolves inside Tor).
stem==1.8.2
PySocks==1.7.1

# AWS & Cloud Storage
boto3>=1.34.0
botocore>=1.34.0
s3transfer>=0.16.1
jmespath==1.0.1
google-cloud-storage==2.5.0

# Firebase & Push Notifications
firebase-admin==5.3.0
django-push-notifications==3.0.0
pyfcm==1.5.4

# Django Channels & WebSockets
channels>=4.0.0,<5
channels-redis>=4.3.0,<5
daphne>=4.2.2,<5  # GHSA-rrc9-mx66-ffcm (CVE-2026-44545): Daphne passed no WebSocket message/frame size limits to Autobahn (whose own defaults are unbounded), letting an unauthenticated client exhaust server memory; fixed in 4.2.2

# GeoIP & Device Detection
geoip2==4.7.0
django-ipware==4.0.2
user-agents==2.2.0

# CLI & Utilities
click==8.3.3
click-didyoumean==0.3.1
click-plugins==1.1.1
click-repl==0.3.0
colorama==0.4.6
prompt_toolkit==3.0.51
wcwidth==0.2.13

# Data Processing
python-dateutil==2.9.0.post0
python-crontab==3.3.0
cron-descriptor==1.4.5
pytz==2025.2
tzdata==2026.2
tzlocal==5.3.1

# XML & Serialization
defusedxml==0.7.1
MarkupSafe==3.0.2
PyYAML==6.0.2
inflection==0.5.1
uritemplate==4.1.1
packaging==25.0
# Build tooling, pulled in transitively. Floored at 83.0.0 for PYSEC-2026-3447
# (a file could bypass an NFC exclusion rule and be packed into a source
# distribution). NOTE this floor governs THIS file only: the prod install
# (Dockerfile.prod / security.yml) resolves requirements-prod.txt under
# requirements-constraints.txt, whose deliberate setuptools<81 ceiling wins
# there — see the rationale beside that pin before "fixing" the mismatch.
# A floor, not an ==: setuptools is a build requirement for other
# packages, so an exact pin here risks conflicting with theirs.
setuptools>=83.0.0

# Encoding
cffi>=2.0.0
pycparser>=2.22
pyasn1==0.6.4
rsa==4.9.1
six==1.17.0

# Environment & Config
python-dotenv==1.2.2

# WSGI/ASGI Server
Werkzeug==3.1.6

# Python 3.13+ Compatibility
legacy-cgi>=2.6.4

# ============================================================================
# Machine Learning & Data Science
# ============================================================================

# Core ML Libraries
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Deep Learning - TensorFlow
tensorflow>=2.13.0

# Deep Learning - PyTorch & Transformers (Behavioral Recovery)
torch>=2.12.0
# No ceiling: capping this at <5.0 (an earlier revision of this file did,
# citing GHSA-fgcw-684q-jj6r / CVE-2026-5241 as a "breaking major release
# we don't need") was itself the bug. pip-audit's CI job resolves this
# floor with no ceiling, which already lands on the latest stable 5.x
# (verified against PyPI, not assumed: transformers 5.17.0 as of
# 2026-09-15) -- a <5.0 cap force-downgrades that to the newest 4.x
# instead, which reintroduces five CVEs whose fixes only ever shipped in
# 5.x (PYSEC-2025-217/CVE-2025-14929, PYSEC-2026-2288/CVE-2026-1839 fixed
# 5.0.0, PYSEC-2026-2289/CVE-2026-4372 fixed 5.3.0, PYSEC-2026-2290/
# CVE-2026-5241 fixed 5.5.0, PYSEC-2026-3929/CVE-2026-9856 fixed 5.10.0) --
# all satisfied by the 5.17.0 the uncapped floor already installs. Keep
# this unbounded; do not reintroduce a <5.0 ceiling.
transformers>=4.35.1

# Quantum Random Number Generation
httpx>=0.27.0
qiskit>=1.0.0
qiskit-ibm-runtime>=0.20.0

# ============================================================================
# Advanced Security Features
# ============================================================================

# Post-Quantum Cryptography
# Note: liboqs-python requires liboqs C library to be compiled and installed.
# In Docker, this is handled by the multi-stage Dockerfile.
# For local development on Windows without Docker, the code will use simulated Kyber.
# liboqs-python>=0.10.0  # Installed from source in Docker

# Pure Python Post-Quantum Crypto (fallback when liboqs unavailable)
# pqcrypto  # Installed separately in Docker

# Blockchain Anchoring
web3>=7.0.0
eth-account>=0.13.0

# Social Mesh Recovery (Shamir's Secret Sharing)
secretsharing>=0.2.6

# Fully Homomorphic Encryption (FHE) - Platform-specific (Linux x86_64 only)
# Note: concrete-python only works on Linux x86_64.
# For other platforms, the code uses fallback encryption mode.
# concrete-python>=2.5.0  # Installed separately in Docker

# ============================================================================
# Notifications & Communication
# ============================================================================

# SMS & Email
twilio>=8.0.0
sendgrid>=6.11.0
authy==2.2.6

# ============================================================================
# Production & Deployment
# ============================================================================

# ASGI/WSGI Servers
gunicorn>=21.2.0,<24
uvicorn[standard]>=0.24.0

# Static Files & Caching
whitenoise>=6.6.0,<7
django-redis>=5.4.0,<7
hiredis>=2.3.2

# Monitoring & Error Tracking
sentry-sdk>=1.40.0
django-prometheus>=2.3.1,<3
psutil>=5.9.0

# Security & Rate Limiting
django-ratelimit>=4.1.0,<5
django-defender>=0.9.7,<1.0

# Health Checks
django-health-check>=3.18.0,<4

# Profiling (Development)
django-silk>=5.0.4,<6

# ============================================================================
# Security Auditing (Development/CI)
# ============================================================================
safety>=3.8.1
pip-audit>=2.6.0

# ============================================================================
# Testing (required in CI/CD)
# ============================================================================
pytest>=8.0.0
# Exact-pinned, not floating: pytest-django 4.13.0 / pytest-asyncio 1.4.0
# (published 2026-08-08) broke pytest-django's own `_django_db_helper`
# fixture with `AttributeError: type object 'PytestDjangoTestCase' has no
# attribute '_pre_setup_ran_eagerly'` on every DB-backed test (203 errors in
# CI). These versions match the already-tested combo in requirements-lock.txt;
# bump deliberately, not by letting `>=` pick up whatever PyPI published today.
pytest-django==4.11.1
pytest-asyncio==1.3.0
pytest-cov>=5.0.0

# ============================================================================
# Optional: MongoDB Support
# ============================================================================
pymongo==4.6.3
aiohttp>=3.14.3 # GHSA-jg22-mg44-37j8 (CookieJar.load RCE) + GHSA-hg6j-4rv6-33pg (cross-origin cookie leak) fixed in 3.14.0; 3.14.1 picks up chunked-decompression / resource-exhaustion hardening (Snyk security upgrade, PR #348); + GHSA-mfx4-hv73-q22v (CVE-2026-69243) WebSocket-upgrade request smuggling, fixed 3.14.2; 3.14.3 adds permessage-deflate / control-frame hardening
authlib>=1.6.12 # not directly required, pinned by Snyk to avoid a vulnerability
filelock>=3.20.3 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.23.1 # not directly required, pinned by Snyk to avoid a vulnerability

# ============================================================================
# Genetic Password Evolution
# ============================================================================
# DNA file parsing (23andMe, AncestryDNA, VCF formats)
biopython>=1.87  # GHSA-x3vf-39hj-gxr4 (Bio.Entrez XXE), fixed in 1.87

# Async HTTP client for DNA provider APIs
# httpx is pinned above (>=0.27.0); duplicate requirements break pip's legacy
# resolver with "Double requirement given".
# httpx>=0.27.0

# ============================================================================
# Chemical Password Storage
# ============================================================================
# Time-lock puzzle math (VDF, modular exponentiation)
sympy>=1.12

# ============================================================================
# Epigenetic Password Adaptation (RL-based)
# ============================================================================
# Reinforcement Learning
gymnasium>=0.29.0
stable-baselines3>=2.0.0

# Contextual Bandits
# vowpalwabbit>=9.0.0; python_version < "3.13" # Use scikit-learn for contextual bandits

# ML utilities
# scikit-learn is pinned above (>=1.3.0); duplicate requirements break pip's
# legacy resolver with "Double requirement given".
# scikit-learn>=1.3.0
python-Levenshtein>=0.21.0

# Differential Privacy (Google's library for formal DP guarantees)
# Note: pydp does not support Python 3.13+. Using last known compatible version.
# For Python 3.13+, the code will use numpy fallback (see adaptive_password_service.py)
# pydp==0.1.16; python_version < "3.13"
diffprivlib>=0.6.4 # IBM's library for Differential Privacy (works on Python 3.13)

# Neural Networks for Memorability Prediction
# Note: torch is pinned above (>=2.2.2) in the Behavioral Recovery block.
# Duplicate entries break pip's legacy resolver ("Double requirement given"),
# so keep only the single authoritative constraint above.
# torch>=2.0.0

# ============================================================================
# Quantum Entanglement-Inspired Key Distribution
# ============================================================================
# Post-Quantum Cryptography (Kyber/NTRU lattice-based)
# liboqs-python>=0.10.0

# Entropy Analysis
# Note: a tighter lower bound appears again in the neuro-feedback block below;
# keep only one bound here (with an upper cap) so pip's resolver does not
# backtrack across duplicate scipy constraints during arm64 builds.
scipy>=1.12.0,<1.14.0

# ============================================================================
# Neuro-Feedback Password Training
# ============================================================================
# EEG device SDKs and signal processing
brainflow>=5.11.0,<5.20.0  # EEG device SDKs (Muse, NeuroSky, OpenBCI)
mne>=1.6.0                 # EEG signal processing & analysis
# scipy pinned once above (entropy analysis block); do not re-declare here.
pylsl>=1.16.0,<1.17.0      # Lab Streaming Layer for EEG
# psychopy>=2024.1.0         # Problem: PsychoPy has NO Python 3.13 Support, # Neuroscience stimulus presentation with biofeedback support
neurokit2>=0.2.0           # Biosignal processing toolkit
# biofeedback>=0.1.0       # Non-existent package for Neurofeedback utilities 

# ============================================================================
# Cognitive Password Testing with Implicit Memory
# ============================================================================
# High-precision timing for reaction time analysis
# performance-timing>=0.1.0  # Performance timing utilities (non-existent package; use time.perf_counter() instead)

# ============================================================================
# Deepfake-Resistant Biometric Liveness
# ============================================================================
# Computer vision and ML for liveness detection
opencv-python-headless>=4.9.0       # Computer vision for face/gaze analysis
mediapipe>=0.10.9          # Face mesh and landmark detection
dlib>=19.24.2              # Facial landmark detection
face-recognition>=1.3.0    # Face embedding and recognition
# torch is pinned above (>=2.2.2); do not redeclare here — legacy pip resolver
# rejects duplicate requirements with "Double requirement given".
# torch>=2.1.0             # PyTorch for deepfake detection
heartpy>=1.2.7             # Heart rate analysis from rPPG

# ============================================================================
# Web Scraping & Dark Web Monitoring
# ============================================================================
scrapy>=2.17.0  # GHSA-76g3-c3x4-crvx (CVE-2026-84366): S3DownloadHandler sent signed s3:// requests over plaintext HTTP by default; fixed in 2.17.0 (HTTPS is now the default transport)

# ============================================================================
# AI-Powered Security Assistant (Claude API)
# ============================================================================
anthropic>=0.40.0

# ============================================================================
# Security overrides for transitive dependencies
# ============================================================================
cbor2>=5.9.0
ecdsa>=0.19.2  # Declined-in-part: CVE-2024-23342 (Minerva P-256 timing attack, GHSA-wj6h-64fc-37mp) is an upstream won't-fix — pure-Python scalar multiplication cannot be made constant-time, and the maintainers explicitly do not recommend this library for production signing/ECDH. This project uses `cryptography`/liboqs for its own crypto; ecdsa is only a transitive dependency. 0.19.2 is still the safest available floor: it fixes CVE-2026-33936 (a DER-parsing truncation bug) and adds partial nonce-bit-size hardening against Minerva.
h2>=4.4.1  # GHSA-6hr6-w5qg-qmwg (CVE-2026-71554): duplicate Host headers were forwarded unchecked, a request-smuggling primitive when an intermediary downgrades HTTP/2 to HTTP/1.1; fixed in 4.4.1
httplib2>=0.32.0  # GHSA-j5g9-f88f-gfj3 (CVE-2026-59939): unbounded gzip/deflate response decompression allowed a small payload to expand to gigabytes in memory (decompression bomb); fixed in 0.32.0 with hard/safe/ratio limits
keras>=3.15.1  # GHSA-5gwj-m78q-7pq3 (CVE-2026-12481) Lambda/TorchModuleWrapper.from_config deserialization bypassing safe_mode when unset, plus the HDF5/tar/zip archive hardening (ExternalLink/SoftLink, virtual datasets, shape bombs, path traversal) shipped across the 3.15.x line
msgpack>=1.2.1  # GHSA-6v7p-g79w-8964 (CVE-2026-57585): reusing an Unpacker after a caught error could trigger an out-of-bounds read/SEGV; fixed in 1.2.1 (still create a new Unpacker after any error other than OutOfData)
nltk>=3.10.3  # GHSA-x99w-6fgc-pmfw (CVE-2026-71513/CVE-2026-79657): AllowlistUnpickler validated only the pickle module, not the name, so dotted-name traversal (sklearn.os.system) and in-namespace gadgets (numpy.f2py.crackfortran.myeval) reached RCE through TransitionParser/Punkt model loading; fixed in 3.10.3. Supersedes the prior 3.9.4 GHSA-gfwx-w7gr-fvh7/GHSA-jm6w-m3j8-898g floor.
pillow>=12.3.0  # Fixes a batch of memory-safety CVEs (WindowsViewer command injection CVE-2026-55798; FontFile/GD decompression-bomb bypasses CVE-2026-54059/-54060/-55379/-55380; JPEG2000/PDF/TGA/EPS/RankFilter/paste-crop/ImageCms OOB reads, writes, and infinite loops CVE-2026-59197/-59199/-59200/-59203/-59204/-59205 and related); fixed in 12.3.0
protego>=0.6.2  # GHSA-wjmf-p669-5m5p (CVE-2026-55520): robots.txt Allow/Disallow wildcards compiled to regex could exponentially backtrack, freezing the crawler on a crafted robots.txt; fixed in 0.6.2 (wildcard matching no longer uses regex)
soupsieve>=2.8.4  # GHSA-2wc2-fm75-p42x (CVE-2026-49476) unbounded memory from large comma-separated selector lists + GHSA-836r-79rf-4m37 (CVE-2026-49477) catastrophic-backtracking ReDoS on unterminated quoted attribute selectors; both fixed in 2.8.4
tornado>=6.5.8  # + CVE-2026-82397/GHSA-mpf4-983q-p7j4: urlencoded body parsing omitted max_num_fields, so one request could stall the event loop; fixed in 6.5.8 (also adds multipart-parts and legacy set_cookie hardening). Prior GHSA-mgf9-4vpg-hj56 / GHSA-3x9g-8vmp-wqvf fixes still apply.
Twisted>=26.4.0  # GHSA-grgv-6hw6-v9g4 (CVE-2026-42304) DNS compression DoS
ujson>=5.12.1  # GHSA-c38f-wx89-p2xg (CVE-2026-44660) memory leak in ujson.dump()
