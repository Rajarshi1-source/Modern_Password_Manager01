from django.shortcuts import render
from django.http import JsonResponse

# Create your views here.
# Note: api_root, router, and urlpatterns are defined in api/urls.py
# Do NOT duplicate them here to avoid conflicting URL configurations.
