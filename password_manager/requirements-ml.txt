# ============================================================================
# Password Manager - Machine Learning & Heavy Scientific Dependencies
# ============================================================================
# Heavy ML/AI/CV deps split from requirements.txt to keep the core image
# buildable on space-constrained CI runners (~14 GB free disk on GitHub
# hosted runners). These are installed in a separate Docker layer that may
# be skipped on space-constrained runners — the application has fallback
# code paths when these libraries are unavailable.
# ============================================================================

# Use CPU-only PyTorch wheels to avoid pulling multi-GB CUDA packages on
# CPU-only runners. Order matters: declare the index first.
--extra-index-url https://download.pytorch.org/whl/cpu

# Core ML Libraries
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Deep Learning - TensorFlow
tensorflow>=2.13.0

# Deep Learning - PyTorch & Transformers (CPU-only wheels via index above)
torch>=2.12.0
# Floor raised to 5.10.0 -- keep in sync with requirements.txt's
# transformers line and its comment there for the full rationale (a
# <5.0 cap previously force-downgraded resolves onto a vulnerable 4.x
# release; a floor below 5.10.0 left the door open for the same thing
# via any future transitive <5.x constraint). No ceiling: do not
# reintroduce a <6.0 cap.
transformers>=5.10.0

# Quantum Random Number Generation
qiskit>=1.0.0
qiskit-ibm-runtime>=0.20.0

# DNA file parsing (23andMe, AncestryDNA, VCF formats)
biopython>=1.87

# Reinforcement Learning
gymnasium>=0.29.0
stable-baselines3>=2.0.0

# String distance metrics
python-Levenshtein>=0.21.0

# IBM's library for Differential Privacy
diffprivlib>=0.6.4

# Entropy Analysis
scipy>=1.12.0,<1.14.0

# EEG device SDKs and signal processing
brainflow>=5.11.0,<5.20.0
mne>=1.6.0
pylsl>=1.16.0,<1.17.0
neurokit2>=0.2.0

# Computer vision and ML for liveness detection
opencv-python-headless>=4.9.0
mediapipe>=0.10.9
dlib>=19.24.2
face-recognition>=1.3.0
heartpy>=1.2.7

# Security overrides for transitive dependencies
h2>=4.4.1  # GHSA-6hr6-w5qg-qmwg (CVE-2026-71554): duplicate Host headers were forwarded unchecked, a request-smuggling primitive when an intermediary downgrades HTTP/2 to HTTP/1.1; fixed in 4.4.1
keras>=3.15.1  # GHSA-5gwj-m78q-7pq3 (CVE-2026-12481) Lambda/TorchModuleWrapper.from_config deserialization bypassing safe_mode when unset, plus HDF5/tar/zip archive hardening shipped across the 3.15.x line
pillow>=12.3.0  # Fixes a batch of memory-safety CVEs (WindowsViewer command injection CVE-2026-55798; FontFile/GD decompression-bomb bypasses; JPEG2000/PDF/TGA/EPS/RankFilter/paste-crop/ImageCms OOB reads, writes, and infinite loops); pulled in transitively by mediapipe/dlib/face-recognition; fixed in 12.3.0
