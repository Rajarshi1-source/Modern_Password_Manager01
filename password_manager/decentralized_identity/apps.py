from django.apps import AppConfig


class DecentralizedIdentityConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "decentralized_identity"
    verbose_name = "Decentralized Identity (DID + W3C VC)"
