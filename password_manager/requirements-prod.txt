# Core Django
Django==5.1.15
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1

# Auth & Security
django-allauth==65.14.1
dj-rest-auth==7.0.1
cryptography==48.0.1  # fix GHSA-537c-gmf6-5ccf (bundled OpenSSL); exact pin keeps it <49 (pyOpenSSL 26.x needs cryptography<49)
argon2-cffi==21.3.0
pycryptodome>=3.20.0
PyJWT==2.13.0
pyotp==2.10.0

# Async & Messaging
celery==5.6.3
redis==7.0.1
django-celery-beat==2.8.0
django_celery_results==2.6.0

# Database — psycopg3
psycopg[binary]==3.2.4

# Channels / WebSockets
channels==4.2.2
channels-redis==4.3.0
daphne==4.2.1

# Cloud & Notifications
firebase-admin==5.3.0
sendgrid>=6.11.0
twilio>=8.0.0

# Web & API
requests==2.33.0
# Snyk SNYK-PYTHON-URLLIB3-* — bumped 2.6.3 → 2.7.0 alongside the
# matching pin in requirements.txt (PR #239). All other pins
# (-core, -lock, ml_dark_web -lock) are bumped in this same commit
# so the production image (which installs requirements-prod.txt)
# actually ships the fixed version, not the pre-bump 2.6.3.
urllib3==2.7.0

# Servers
gunicorn>=21.2.0
uvicorn[standard]>=0.24.0
whitenoise>=6.6.0

# Monitoring
sentry-sdk>=1.40.0
psutil>=5.9.0

# ML Runtime (if enabled)
torch>=2.12.0
transformers>=5.3.0
numpy>=2.2.0
pandas>=2.2.0
scikit-learn>=1.4.0
