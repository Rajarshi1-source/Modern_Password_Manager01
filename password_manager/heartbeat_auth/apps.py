from django.apps import AppConfig


class HeartbeatAuthConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'heartbeat_auth'
    verbose_name = 'Heartbeat / HRV Authentication'
