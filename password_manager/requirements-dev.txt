# Local development, testing, CI
-r requirements-prod.txt

# Testing
pytest>=9.0.0
# Exact-pinned: see requirements.txt for why (pytest-django 4.13.0 /
# pytest-asyncio 1.4.0 broke pytest-django's own fixture internals).
pytest-django==4.11.1
pytest-asyncio==1.3.0
factory-boy>=3.3.0

# Security Audits
pip-audit>=2.10.0
safety>=3.8.1

# Profiling & Debugging
django-silk>=5.4.3
django-extensions>=4.1

# CLI & Utilities
click>=8.1.8
rich>=14.0.0

# Web Scraping (Dev / ML)
scrapy>=2.17.0,<3.0  # GHSA-76g3-c3x4-crvx (CVE-2026-84366): S3DownloadHandler sent signed s3:// requests over plaintext HTTP by default; fixed in 2.17.0
protego>=0.6.2  # GHSA-wjmf-p669-5m5p (CVE-2026-55520): robots.txt Allow/Disallow wildcards compiled to regex could exponentially backtrack, freezing the crawler on a crafted robots.txt; fixed in 0.6.2 (transitive via scrapy)
beautifulsoup4>=4.13.0
soupsieve>=2.8.4  # GHSA-2wc2-fm75-p42x (CVE-2026-49476) unbounded memory from large comma-separated selector lists + GHSA-836r-79rf-4m37 (CVE-2026-49477) catastrophic-backtracking ReDoS on unterminated quoted attribute selectors; both fixed in 2.8.4 (transitive via beautifulsoup4)
selenium>=4.39.0
PySocks>=1.7.1

# Pre-commit hooks & secret detection
pre-commit>=4.0.0
detect-secrets>=1.5.0
